---
name: learning-tracker
description: 管理学习计划、追踪学习进度、记录每日打卡、间隔复习与出题考察。当用户提到学习计划、学习进度、打卡、记录学习、我今天学了、查看进度、学习报告、复习、考我、连续打卡、调整计划时使用此技能。不用于代码进度/任务管理/日程提醒。
---

# Learning Tracker

数据在当前项目的 `.learning/`，读写都走 `scripts/tracker.py`，不要手改 JSON（`plan.json` 除外，它是给人编辑的）。

```bash
python3 scripts/tracker.py init "目标" 2026-09-20 2027-09-20 3.5
python3 scripts/tracker.py add "线代直觉" 8 --dod "不查资料说出 Linear 的输出 shape"
python3 scripts/tracker.py dod task-1 "..."                 # 补/改完成标准
python3 scripts/tracker.py checkin task-1 "看了3B1B第3集" 45 [--done] [--notes "..."]
python3 scripts/tracker.py ask task-1 "问题" "标准答案"       # 学完立刻出题
python3 scripts/tracker.py quiz [--limit 5]                 # 到期的题，不含答案
python3 scripts/tracker.py answer q-1                       # 判卷时才看答案
python3 scripts/tracker.py grade q-1 ok|fail
python3 scripts/tracker.py report
```

总时长、连续打卡、掌握度全部由记录实时推导，**没有需要同步的第二份统计**。
复习间隔阶梯 1/3/7/21/60 天：答对进一级，答错回第一级并计入 `fails`。

## 核心回路

**学 → 出题 → 到期重问 → 答错的自动加密。** 只记时长不出题，这东西就退化成计时器。

## 五个场景

**制定计划**（"帮我制定学习计划"）
1. 先定方向——目标往往比用户说的更含糊（"算法工程师"是四五个不同岗位），选错方向后面全白费。
2. **起点靠考，不靠问。** "你会 X 吗"的答案永远是"会一点"，没有信息量。直接出 5–8 道产出型判定题（"不查文档写出一个训练循环，并说出 zero_grad 放错会怎样"），让用户答不上来就写"不会"。
3. **预算和目标不匹配必须直说。** 算清总小时数，说出它实际能买到什么。不说的规划器只是在生产计划形状的顺从。
4. **优先把已有的课程产物转成任务**（仓库 README、教材目录），不要凭空编——编出来的会和真实材料脱节。
5. **任务用完成标准定义，不用小时数定义。** `--dod` 写"能做出什么"（能手推 / 能空白文件写出 / 能不看原文重写），不写"学习 X 十小时"。DoD 同时就是将来的考题。
6. **只建最近 1–2 个阶段的任务。** 四个月后的估时是假的，到时候重估。远期路线写进 `.learning/roadmap.md`（纯文档，工具不读）。

**材料筛选**：标准是"有没有让你产出东西"——有习题、有代码要写的排前面，纯视频只配补漏。
**链接必须当天用 WebFetch 验一遍**，并按 `名字 + 作者 + 版本` 记录：模型对 URL、章节号、课程是否还在线的记忆不可靠，会编出看起来很对的死链。

**打卡**（"打卡" / "我今天学了X"）：提取任务、内容、时长。对不上任务就 `report` 看列表再问；时长没说就问，不要猜。
打卡后**必须追问一句能不能出题**，用户学到了东西就 `ask` 存 1–3 道——这是整个系统的价值所在。
只有 DoD 真的达成才 `--done`，达成与否由你当场考一遍确认，不是用户说完了就算。

**复习**（"复习" / "考我" / 或 report 显示有到期题）：`quiz` 取题 → **在对话里问出来，等用户作答** → `answer` 看标准答案 → 判对错 → `grade`。
不要把答案和问题一起甩给用户，那就退回成"再读一遍"，提取练习的效果全没了。

**看进度**（"查看进度" / "我学到哪了"）：`report`，然后用一两句话点出真正有用的信息——落后多少、哪个任务卡住、"反复答错"里暴露了什么——而不是复述数字。

**调整计划**（"加快/放慢"）：直接改 `plan.json` 的 `end_date`/`daily_hours`/估时，改完 `report` 确认。打卡记录是流水账，永远不要改。

## 注意事项

1. 只在用户说打卡时才记录，不要主动催
2. **报告里的数字必须如实说，不许为了鼓励而粉饰。** 这个系统能发出的最有价值的一句话恰恰是负反馈：「你三周前说搞懂了矩阵乘法，刚才答不上来」。连胜断了、估时滑了、某个概念错了四次——都要说。鼓励放在如实之后，不是替代它。
3. 连续打卡天数不该排在报告第一行。那是留存指标，你的目标是东西留在脑子里，所以待复习和掌握度优先。
4. 数据只存本地
